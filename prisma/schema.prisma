datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id           Int           @id @default(autoincrement())
  email        String?       @unique @db.Citext
  login        String        @unique
  password     String
  refreshToken String?
  role         String        @default("USER") 
  isEmailVerified Boolean    @default(false)
  emailVerificationToken String?
  emailVerificationExpires DateTime?
  passwordResetToken String?
  passwordResetExpires DateTime?
  passwordResetCode String?
  passwordResetCodeExpires DateTime?
  books        Book[]
  collections  Collection[]
  progress     ReadingProgress[]
  bookmarks    Bookmark[]
  notes        Note[]
  ratings      Rating[]
  viewHistory  ViewHistory[]
}

model Book {
  id               Int             @id @default(autoincrement())
  title            String
  author           String?
  format           String
  publisher        String?
  language         String?
  storagePath      String?         @unique
  filePath         String          @unique
  originalFilePath String?
  genre            String?
  createdAt        DateTime        @default(now())
  updatedAt        DateTime        @updatedAt
  isPublic         Boolean         @default(false)
  imageUrl         String?
  avgRating        Float?          @default(0.0)
  externalBookId   Int?            // Link to external book source
  
  tags             Tag[]           @relation("BookToTags")
  collections      Collection[]
  bookmarks        Bookmark[]
  notes            Note[]
  progress         ReadingProgress[]
  ratings          Rating[]
  viewHistory      ViewHistory[]
  user             User?           @relation(fields: [userId], references: [id])
  userId           Int?
  externalBook     ExternalBook?   @relation("ExternalToBook", fields: [externalBookId], references: [id])
}

model Tag {
  id    Int     @id @default(autoincrement())
  name  String  @unique
  books Book[]  @relation("BookToTags")
}

model ReadingProgress {
  id        Int      @id @default(autoincrement())
  progress  Float
  position  String
  updatedAt DateTime @default(now())
  
  book      Book     @relation(fields: [bookId], references: [id])
  bookId    Int
  user      User     @relation(fields: [userId], references: [id])
  userId    Int

  @@unique([bookId, userId])
}

model Bookmark {
  id        Int      @id @default(autoincrement())
  position  String
  note      String?
  createdAt DateTime @default(now())
  
  book      Book     @relation(fields: [bookId], references: [id])
  bookId    Int
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
}

model Note {
  id        Int      @id @default(autoincrement())
  content   String
  position  String
  createdAt DateTime @default(now())
  
  book      Book     @relation(fields: [bookId], references: [id])
  bookId    Int
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
}

model Collection {
  id        Int      @id @default(autoincrement())
  name      String
  createdAt DateTime @default(now())
  
  books     Book[]
  user      User?    @relation(fields: [userId], references: [id])
  userId    Int?
}

model Rating {
  id        Int      @id @default(autoincrement())
  value     Float    @default(0.0)
  createdAt DateTime @default(now())
  
  book      Book     @relation(fields: [bookId], references: [id])
  bookId    Int
  user      User     @relation(fields: [userId], references: [id])
  userId    Int

  @@unique([bookId, userId])
}

model ViewHistory {
  id        Int      @id @default(autoincrement())
  viewedAt  DateTime @default(now())
  
  book      Book     @relation(fields: [bookId], references: [id])
  bookId    Int
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
}

model ExternalBook {
  id                Int      @id @default(autoincrement())
  externalId        String
  source            String   // 'google_books', 'open_library', 'gutenberg'
  title             String
  author            String?
  description       String?
  isbn              String?
  publishedDate     String?
  publisher         String?
  language          String?
  pageCount         Int?
  categories        String[] // Array of categories/genres
  imageUrl          String?
  downloadUrl       String?
  previewUrl        String?
  isDownloadable    Boolean  @default(false)
  isPublicDomain    Boolean  @default(false)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  lastCheckedAt     DateTime @default(now())
  
  // Relations
  importedBooks     Book[]   @relation("ExternalToBook")

  @@unique([externalId, source])
}